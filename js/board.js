class Board {
    constructor(x, y) {
        this.pos = new Vector(x, y);
        this.grid = [];
        this.shapes = [];
    }

    createShape(type, color) {
        // todo: create shape in random position in the top of the board

    }
}